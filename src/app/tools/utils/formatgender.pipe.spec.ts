import { FormatgenderPipe } from './formatgender.pipe';

describe('FormatgenderPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatgenderPipe();
    expect(pipe).toBeTruthy();
  });
});
